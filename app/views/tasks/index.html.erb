<h1>トップページ</h1>

<%= link_to "新規作成", new_task_path %>

<div class="task-list">
  <% @tasks.each do |t| %>
    <div class="task-content">
      <%= link_to t.task_title, task_path(t.id), class:"task-link" %>
    </div>
  <% end %>
</div>